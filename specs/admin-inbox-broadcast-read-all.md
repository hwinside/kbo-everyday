# 어드민 쪽지함: 단체발송 숨김 · 모두 읽음

요청: 2026-09-09 #cs `1788917233.098669`.

## 범위
- 운영팀 수신함 목록에서 단체발송 사본을 제외한다. 접힘/그룹화하지 않는다.
- 기존 개별 대화는 마지막 개별 메시지 시각/미리보기로 정렬한다. 단체발송 뒤 들어온 사용자 답장은 정상 노출한다.
- 단체발송은 큐 발송의 서버 전용 `dedup_key=admin-broadcast:...`로 식별한다. 본문 유사도로 판단하지 않는다.
- 건의함 회신, 사진 쪽지, 탈퇴 사용자 대화는 기존 노출 계약을 유지한다.
- 사용자 쪽지함과 대화 원문, 공유 미리보기, 발송함 집계, 발송 큐에는 변경하지 않는다.
- 모두 읽음은 현재 페이지와 무관하게 운영팀이 받은 전체 안읽은 메시지에 적용한다. 운영팀이 보낸 쪽지의 사용자 읽음 여부와 일반 사용자 간 대화는 변경하지 않는다.
- 처리 SQL 스냅샷 이후 도착한 쪽지는 안읽음으로 남는다. 중복 처리는 멱등이다.

## 구현 / 배포 순서
1. `20260909110000_admin_inbox_broadcast_read_all.sql` 적용: 관리자 목록 RPC 교체, 부분 인덱스, service_role 전용 전체 읽음 RPC.
2. API/UI 배포. API의 기존 관리자 인증을 통과해야 하며 운영팀 ID는 서버 설정에서만 가져온다.
3. 처리 후 목록과 사이드바 카운트를 재조회한다. 오류는 성공으로 표시하지 않는다.

## 작성자 self-check
- `node --test tests/admin-inbox.test.mjs tests/admin-inbox-api.test.mjs`
- 격리 PGlite 실제 SQL: 단체발송 186건 보존, 단체발송 전용/혼합 대화, 50건 초과 커서, 사용자 순서 양방향, 피드백/사진/탈퇴 사용자, 전역 읽음, 타인 대화/수신자 읽음 보존, 이후 신규 메시지, 권한 거절, 원자적 실패 롤백.
- API 실제 핸들러 + auth/DB mock: 미인증, 설정 누락, 클라이언트 범위 위조 무시, RPC 오류/응답 오류, 멱등 0건 성공.
- 변경 파일 eslint, 전체 TypeScript 검사.
- 이 결과는 독립 QA 완료 근거가 아니다.

## 삼식 리뷰 / 전용 계정 QA
- exact SHA 코드·SQL 리뷰 및 저장소 필수 checks/build.
- 미적용 DB에서는 신규 버튼이 오류 처리되는지, migration 적용 후 성공하는지.
- 어드민 실제 목록/클릭/사이드바 갱신, 더보기 중 처리, polling 응답 역전, 중복 클릭·실패 후 재시도.
- 전용 사용자 2계정의 쪽지함/단체발송/개별 답장/읽음 상태와 비어드민 접근 차단.
- 실서버 migration과 실제 UI는 작성자 self-check와 별도 판정한다.

## 롤백
- UI/API revert로 버튼을 제거한다. 새 RPC/인덱스는 호환되므로 즉시 삭제할 필요 없다.
- 목록을 되돌려야 하면 `20260727_auth_user_delete_cascades.sql`의 이전 `admin_dm_inbox_page` 정의와 service_role 권한을 복원한다.
- 읽음 처리는 사용자 의도에 따른 데이터 변경이므로 무차별 안읽음 역변환을 하지 않는다.
