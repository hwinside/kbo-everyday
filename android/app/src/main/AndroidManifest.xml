<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Android 11+(API 30) package visibility: Custom Tabs provider(Chrome 등)를 조회하려면
         이 queries 선언이 필요. 없으면 OAuthBrowserPlugin의 CustomTabsClient.getPackageName이
         null을 반환해 삼성 인터넷 기본 브라우저로 폴백 → Chrome 강제 fix가 무력화됨(삼순 리뷰). -->
    <queries>
        <intent>
            <action android:name="android.support.customtabs.action.CustomTabsService" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep Links: Android App Links for OAuth callback.
                 /auth/callback 경로로 한정 — keubo.fan 전체를 잡으면 네이버 중간 콜백
                 (/api/auth/naver/callback?code=)까지 앱이 가로채 서버 토큰 교환이 안 돌고
                 네이버 code를 Supabase PKCE로 잘못 교환해 로그인 실패함. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="keubo.fan" android:pathPrefix="/auth/callback" />
            </intent-filter>

            <!-- Custom scheme deep link for OAuth callback (iOS 커스텀 스킴과 동일).
                 위 https App Link는 기기 App Link 검증에 의존 — 미검증 기기에선 콜백이 앱 대신
                 Chrome에서 처리돼 세션이 Chrome에만 남는다(크롬만 로그인/앱 로그아웃, #cs 2026-07-09).
                 커스텀 스킴은 검증이 필요 없어 항상 앱으로 전달되므로 이걸 OAuth 콜백 기본 경로로 쓴다.
                 appUrlOpen(capacitor/auth.ts)이 fan.keubo.app:// 를 이미 가로채 code/token 교환함. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="fan.keubo.app" android:host="auth" android:pathPrefix="/callback" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <!-- 홈 화면 App Widget — 실시간 경기 스코어 (A4 B3) -->
        <receiver
            android:name=".GameScoreWidget"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/game_score_widget_info" />
        </receiver>

        <!-- 스몰(2x2) 위젯 — iOS systemSmall 등가. GameScoreWidget과 prefs 공유, 렌더만 컴팩트. -->
        <receiver
            android:name=".GameScoreWidgetSmall"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/game_score_widget_small_info" />
        </receiver>

        <!-- 팀 순위표 위젯(4x4) — 앱 순위표 동일 디자인 + 최애팀 하이라이트. 자체 fetch + 캐시. -->
        <receiver
            android:name=".TeamRankWidget"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/team_rank_widget_info" />
        </receiver>

        <!-- 최애선수 스탯 카드 위젯(4x3) — 홈 최애선수 카드 동일 디자인, config에서 선수 선택. -->
        <receiver
            android:name=".PlayerCardWidget"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/player_card_widget_info" />
        </receiver>
        <activity
            android:name=".PlayerCardWidgetConfigure"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <!-- FCM 수신 서비스 — capawesome MessagingService를 확장(앱 꺼져도 라이브 푸시 →
             잠금화면 ongoing notification + 위젯). 플러그인 기본 서비스는 제거하고 이걸로 대체. -->
        <service
            android:name=".KboMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <service
            android:name="io.capawesome.capacitorjs.plugins.firebase.messaging.MessagingService"
            tools:node="remove" />

        <!-- FCM notification 페이로드 푸시도 모노크롬 small icon 사용(흰 사각형 방지). -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_stat_kbo" />

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />
        <meta-data
            android:name="com.facebook.sdk.ApplicationName"
            android:value="@string/facebook_display_name" />
        <meta-data
            android:name="com.facebook.sdk.AutoInitEnabled"
            android:value="true" />
        <meta-data
            android:name="com.facebook.sdk.AutoLogAppEventsEnabled"
            android:value="true" />
        <meta-data
            android:name="com.facebook.sdk.ClientToken"
            android:value="@string/facebook_client_token" />
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
